import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex:1,
        padding: 10,
        },
        textTitle: {
            fontFamily: "bold",
            textAlign: "center",
            fontSize: 32,
            padding: 16,
        },
        input: {
            padding: 10,
            borderWidth: 1
        }
  });
  