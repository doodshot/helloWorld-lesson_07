import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 16,
    borderWidth: 2,
    width: 20,
    height: 20,
    borderRadius: 10,
  },
  title: {
    fontSize: 24,
    textAlign: "center",
    color: "blue"
  },
  addBtn: {
    padding: 16,
    borderWidth: 2,
    borderRadius: 10,
    borderColor: "blue",
  }
});
